import Image from "next/image";

export default function page() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-between p-24">
      vendor page
      <header>
        <button>Already vendor</button>
      </header>
      <header>
        <button>Become a vendor</button>
      </header>
    </main>
  );
}
